[package]
name = "dezh-identity"
version = "0.1.0"
edition = "2021"
description = "Dezh Step 3 — principals, delegation, invocations, and provenance chains."

[dependencies]
blake3 = "1"

[lib]
path = "src/lib.rs"
